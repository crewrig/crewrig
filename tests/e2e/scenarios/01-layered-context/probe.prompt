What is the user's location according to the profile rule files loaded
into your context? Answer in exactly one word — the city name — and
write your answer (and nothing else) to /out/answer.txt before exiting.
