---
name: probe-canonical
description: "Fixture agent source for the spec 0198 resolver test suite. Declares the canonical example's capability profile (spec 0195: intelligence medium x reasoning medium) — never installed, never compiled: this directory is outside artifacts/, so no tier discovery, no drift guard and no install path ever sees it."
metadata:
  model:
    intelligence: medium
    reasoning: medium
---
# Fixture Body

Not a real agent; used only by scripts/tests/test-model-resolution.sh.
