# test-wiring-exemptions.txt — allowlist of scripts/tests/test-*.sh scripts that
# are deliberately NOT executed by any CI workflow (spec 0076).
#
# Format, one entry per line:
#
#   <test-name.sh><TAB><human-readable reason>
#
# Blank lines and lines starting with '#' are ignored. Enforced by
# scripts/check-test-wiring.sh:
#   - every entry MUST carry a reason (an empty reason fails the check, R3);
#   - every entry MUST name a test that still exists under scripts/tests/
#     (a stale entry fails the check, R5).
#
# Hygiene: when a quarantined (bug-exempted) test is later fixed and wired into
# a workflow, DELETE its line here. A test that is both wired and exempted is a
# redundant exemption — the guard tolerates it, but it is dead weight and hides
# the fact that the underlying bug is resolved.

# --- Environment-dependent (cannot run in the hermetic check-components job) ---
test-e2e-auth-scripts.sh	exercises scripts/e2e/auth-copilot.sh, which e2e_die's when ~/.copilot/instructions/ is absent — a `task setup:copilot` side-effect present on a developer host but not in a clean CI HOME; cannot pass in the hermetic check-components job. See follow-up note from #534 on whether the guidance path should print before that precondition.
